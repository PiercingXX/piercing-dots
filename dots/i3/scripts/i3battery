#!/bin/bash
# i3blocks battery script

# Show battery status using acpi, fallback to upower if not available
if command -v acpi >/dev/null 2>&1; then
    acpi -b | awk -F', ' '{print $2 " (" $3 ")"}'
else
    upower -i $(upower -e | grep BAT) | awk '/percentage/ {print $2}'
fi
