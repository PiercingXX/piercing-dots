# GitHub.com/PiercingXX

####################
### KEYBINDINGS ###
####################
$terminal = kitty
$filemanager = yazi
$browser = waterfox
$launcher = ulauncher
$shiftMod = SHIFT
$ctrlMod = CTRL
$altMod = ALT
$mainMod = SUPER


#Essentials
bind = ,                            $mainMod,           exec, true # Open app launcher
bindr = $mainMod,                   $mainMod_L,         exec, ulauncher # Open ulauncher
#bindr = $mainMod,                   $mainMod_L,         exec, pkill fuzzel || fuzzel # Open fuzzel
bind = $mainMod,                    Q,                  killactive # Kill active window
bind = ALT,                         TAB,                exec, nwg-drawer -c 7 -is 70 -spacing 13
bind = $mainMod SHIFT,              S,                  exec, pavucontrol # Launch pavucontrol (volume mixer)
bind = $mainMod ALT,                S,                  exec, XDG_CURRENT_DESKTOP="gnome" gnome-control-center # Launch GNOME Settings
bind = $mainMod,                    S,                  exec, kitty ~/.scripts/PiercingXX-Settings-Menu/settings-menu.sh # Open settings menu
bind = $mainMod SHIFT,              F,                  fullscreen # Toggle fullscreen mode
bind = $mainMod,                    F,                  togglefloating # Toggle floating mode
bind = $mainMod,                    R,                  exec, pkill waybar && waybar &
bind = ,                            PRINT,              exec, hyprshot -m region # Screenshot a region
bind = $shiftMod,                   PRINT,              exec, hyprshot -m window # Screenshot a window
bind = $mainMod,                    X,                  layoutmsg, togglesplit # Toggle split mode


# Clipboard History
bind = $mainMod SHIFT,              H,                  exec, pkill fuzzel || cliphist list | fuzzel  --match-mode fzf --dmenu | cliphist decode | wl-copy # Clipboard history >> clipboard


# Notes and To-Do Lists
bind = $mainMod,                    N,                  exec, kitty ~/.scripts/Note-Scripts/open_daily_note.sh --split # Open To-Do List next to daily note
bind = $mainMod SHIFT,              N,                  exec, kitty ~/.scripts/Note-Scripts/open_daily_note.sh --view # Open daily note to view
bind = $mainMod ALT,                N,                  exec, kitty ~/.scripts/Note-Scripts/open_daily_note.sh --todo # Open To-Do List
bind = $mainMod SHIFT+ALT,          N,                  exec, kitty ~/.scripts/Note-Scripts/open_daily_note.sh --directory # Open Yazi in Daily Notes Directory
bind = $mainMod,                    I,                  exec, kitty ~/.scripts/Note-Scripts/inventory.sh # Open inventory directory


# Server Monitor
bind = $mainMod,                    M,                  exec, kitty -o allow_remote_control=yes -o close_on_child_death=yes ~/.scripts/Control-Scripts/launch-server-monitor.sh -monitor
bind = $mainMod SHIFT,              M,                  exec, kitty -o allow_remote_control=yes -o close_on_child_death=yes ~/.scripts/Control-Scripts/launch-server-monitor.sh -remote


# Applications
bind = $mainMod,                    W,                  exec, kitty # Open kitty
bind = $mainMod,                    T,                  exec, kitty tmux # Open tmux inside kitty
bind = $mainMod SHIFT,              Z,                  exec, nautilus --new-window # Open nautilus
bind = $mainMod,                    Z,                  exec, kitty yazi # Open yazi
bind = $mainMod,                    C,                  exec, waterfox || flatpak run net.waterfox.waterfox # Internet Browser
bind = $mainMod,                    D,                  exec, discord || flatpak run com.discordapp.Discord # Discord
bind = $mainMod SHIFT,              V,                  exec, code # Visual Studio Code
bind = $mainMod,                    V,                  exec, kitty nvim
bind = $mainMod,                    B,                  exec, obsidian || flatpak run md.obsidian.Obsidian # Obsidian
bind = $mainMod,                    G,                  exec, gimp || flatpak run org.gimp.GIMP
bind = Ctrl+Alt,                    Delete,             exec, kitty flatpak run io.missioncenter.MissionCenter
bind = Ctrl+ALT+SHIFT,              K,                  exec, sudo shutdown -h now


# Show Keybinds
bind = $mainMod,                    slash,              exec, xdg-open "/home/$USER/.scripts/Control-Scripts/keybinds.html"


# Self Hosted AI
bind = $mainMod,                    A,                  exec, kitty ~/.scripts/Control-Scripts/launch-ollama-remote.sh --skippy
bind = $mainMod SHIFT,              A,                  exec, kitty ~/.scripts/Control-Scripts/launch-ollama-remote.sh --gpt
bind = $mainMod ALT,                A,                  exec, kitty ~/.scripts/Control-Scripts/launch-ollama-remote.sh --gemma


#Session
bind = $mainMod,                    grave,              exec, hyprlock # Lock screen
bindl = $mainMod+Shift,             grave,              exec, sleep 0.1 && systemctl suspend || loginctl suspend # Suspend system
#bind = Ctrl+Shift+Alt,              Delete,             exit # Logout - Hope you saved your work!
bind = Ctrl+Shift+Alt+$mainMod,     Delete,             exec, systemctl poweroff || loginctl poweroff # Power off


#Workspace navigation
bindm = $mainMod,                   mouse:272,          movewindow # Mouse move window
bindm = $mainMod,                   Control_L,          movewindow # Control move window
bindm = $mainMod,                   Alt_L,              resizewindow # Resize window
#Move focus with mainMod + vim keys
bind = $mainMod,                    H,                  movefocus, l
bind = $mainMod,                    J,                  movefocus, d
bind = $mainMod,                    K,                  movefocus, u
bind = $mainMod,                    L,                  movefocus, r
#Move window with mainMod + vim keys
bind = $mainMod SHIFT,              H,                  movewindow, l
bind = $mainMod SHIFT,              J,                  movewindow, d
bind = $mainMod SHIFT,              K,                  movewindow, u
bind = $mainMod SHIFT,              L,                  movewindow, r
#Resize Window with mainMod + vim keys
bind = $mainMod ALT,                H,                  resizeactive, -100 0
bind = $mainMod ALT,                J,                  resizeactive, 0, 100
bind = $mainMod ALT,                K,                  resizeactive, 0, -100
bind = $mainMod ALT,                L,                  resizeactive, 100 0
# # Move focus with mainMod + arrow keys
# bind = $mainMod,                    left,               movefocus, l # Focus left
# bind = $mainMod,                    right,              movefocus, r # Focus right
# bind = $mainMod,                    up,                 movefocus, u # Focus up
# bind = $mainMod,                    down,               movefocus, d # Focus down
# # Move window with mainMod + arrow keys
# bind = $mainMod SHIFT,              left,               movewindow, l # Move window left
# bind = $mainMod SHIFT,              right,              movewindow, r # Move window right
# bind = $mainMod SHIFT,              up,                 movewindow, u # Move window up
# bind = $mainMod SHIFT,              down,               movewindow, d # Move window down
# # Resize Window with mainMod + vim keys
# bind = $mainMod ALT,                left,               resizeactive, -100 0 # Resize window left
# bind = $mainMod ALT,                right,              resizeactive, 100 0 # Resize window right
# bind = $mainMod ALT,                up,                 resizeactive, 0, -100 # Resize window up
# bind = $mainMod ALT,                down,               resizeactive, 0, 100 # Resize window down



#Switching
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10


#Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10


bind = $mainMod+Alt, 1, movetoworkspacesilent, 1
bind = $mainMod+Alt, 2, movetoworkspacesilent, 2
bind = $mainMod+Alt, 3, movetoworkspacesilent, 3
bind = $mainMod+Alt, 4, movetoworkspacesilent, 4
bind = $mainMod+Alt, 5, movetoworkspacesilent, 5
bind = $mainMod+Alt, 6, movetoworkspacesilent, 6
bind = $mainMod+Alt, 7, movetoworkspacesilent, 7
bind = $mainMod+Alt, 8, movetoworkspacesilent, 8
bind = $mainMod+Alt, 9, movetoworkspacesilent, 9
bind = $mainMod+Alt, 0, movetoworkspacesilent, 10


#Media control
bindl = , XF86AudioPlay,                                exec, playerctl --all-players play-pause                                                # Pause audio/video
binde = , XF86AudioNext,                                exec, playerctl next                                                                    # Next track
binde = , XF86AudioPrev,                                exec, playerctl previous                                                                # Previous track
binde = , XF86AudioRaiseVolume,                         exec, ~/.scripts/Control-Scripts/volume.sh inc                                          # Increase Volume
binde = , XF86AudioLowerVolume,                         exec, ~/.scripts/Control-Scripts/volume.sh dec                                          # Decrease Volume
binde = , XF86AudioMicMute,                             exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle                                     # Mute microphone
bindl = , XF86AudioMute,                                exec, ~/.scripts/Control-Scripts/volume.sh mute                                         # Mute audio
# Audio Output/Input Manager
bind = $mainMod,                    O,                  exec, ~/.scripts/PiercingXX-Settings-Menu/audio-input-manager.sh --toggle-output        # Toggle audio output



#Brightness control
bind = , code:232, exec, ~/.scripts/Control-Scripts/brightness.sh dec
bind = , code:233, exec, ~/.scripts/Control-Scripts/brightness.sh inc

# Touchpad gestures (new gesture system - https://wiki.hypr.land/Configuring/Gestures/)
gesture = 3, horizontal, workspace                                                                              # 3-finger horizontal swipe to switch workspaces
gesture = 3, down, close                                                                                        # 3-finger swipe down to close window
gesture = 3, up, fullscreen                                                                                     # 3-finger swipe up to toggle fullscreen
gesture = 4, up, dispatcher, exec, ulauncher                                                                    # 4-finger swipe up to launch ulauncher
#gesture = 4, down, close                                                                                        # 4-finger swipe down to close window

# Touchscreen and touchpad gestures (hyprgrass plugin)
bind = , edge:d:u, exec, ulauncher                                                                              # 3-finger swipe up from bottom edge to launch ulauncher
bind = , swipe:3:l, workspace, e+1                                                                              # 3-finger swipe left to next workspace
bind = , swipe:3:r, workspace, e-1                                                                              # 3-finger swipe right to previous workspace
bind = , swipe:3:d, exec, pkill ulauncher                                                                       # 3-finger swipe down to close ulauncher
#bind = , swipe:4:d, killactive                                                                                  # 4-finger swipe down to close window
bind = , swipe:4:u, exec, ulauncher                                                                                # 4-finger swipe up to toggle fullscreen
bind = , tap:3, exec, pkill fuzzel || cliphist list | fuzzel --match-mode fzf --dmenu | cliphist decode | wl-copy  # 3-finger tap for clipboard history

